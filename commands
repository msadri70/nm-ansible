ansible <hosts> [-m <module_name>] -a <"arguments"> -u <username> [--become]

ansible all -m ping -i hosts
ansible all -a uptime -i hosts
ansible all -m shell -a "uname -a" -i hosts
ansible all -m command -a lscpu -i hosts


ansible web -m user -a 'name=user1 password=somepassword' -b -K -i hosts
